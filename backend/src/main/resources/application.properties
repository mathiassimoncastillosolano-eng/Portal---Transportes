spring.application.name=backend

spring.datasource.url=jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.csirjxouaquvtxpwuitd
spring.datasource.password=Tours_DB2026

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ---------------------------------------------------------------------------
# Configuracion de JWT
# ---------------------------------------------------------------------------
# Clave secreta utilizada para firmar los JWT con el algoritmo HS256.
# IMPORTANTE (produccion): sobrescribir SIEMPRE mediante la variable de
# entorno JWT_SECRET con un valor propio, largo y aleatorio (minimo 256 bits).
# El valor por defecto solo existe para que el proyecto funcione de inmediato
# en un entorno de practica/estudio.
security.jwt.clave-secreta=${JWT_SECRET:R7k2pL9xQ4vT8mN1sB6wA3dF0hJ5yC2eZ9uI4oP7rE1tY6qW3nM8gH0jK5xL2vD9}

# Tiempo de vida del token, en minutos.
security.jwt.expiracion-minutos=${JWT_EXPIRACION_MINUTOS:60}

# ---------------------------------------------------------------------------
# CORS: origen del frontend (Vite) que consumira la API durante el desarrollo.
# ---------------------------------------------------------------------------
seguridad.cors.origenes-permitidos=${CORS_ORIGENES:http://localhost:5173}
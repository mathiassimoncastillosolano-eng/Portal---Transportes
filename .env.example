# URL base del backend de RutaLibre (Spring Boot).
# Copia este archivo como ".env" y ajusta el valor si el backend corre en
# otro host o puerto. Vite solo expone variables que empiecen con VITE_.
VITE_API_URL=http://localhost:8080
